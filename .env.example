# =============================================================================
# Nostria - Test Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values for your test environment.
# The .env file is gitignored and should never be committed.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   npm run test:e2e
# =============================================================================

# -----------------------------------------------------------------------------
# Nostr Test Account
# -----------------------------------------------------------------------------

# Private key for the test account (nsec1... bech32 format)
# Used for authenticated E2E tests (login, posting, profile editing, etc.)
# Generate one at: https://nostrtool.com or using nostr-tools
# WARNING: Use a dedicated test account - never use your real private key!
#TEST_NSEC=nsec1...

# Hex-encoded public key for the test account
# If omitted, it will be automatically derived from TEST_NSEC
#TEST_PUBKEY=

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------

# Base URL of the Nostria app instance to test against
# Default: http://localhost:4200
#BASE_URL=http://localhost:4200

# -----------------------------------------------------------------------------
# Test Configuration
# -----------------------------------------------------------------------------

# Log verbosity for test output
# Options: debug | info | warn | error
# Default: info
#TEST_LOG_LEVEL=info

# Set to true in CI/CD environments
# Affects: retries (2 in CI vs 1 local), worker count (1 in CI), forbidOnly,
# and reuseExistingServer (disabled in CI)
#CI=false
