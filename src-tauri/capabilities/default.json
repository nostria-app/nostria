{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window across supported targets.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "android-signer:default",
    "media-session:default",
    "opener:default",
    "updater:default"
  ]
}